let mongoUser ="hallucinatronic";
let mongoPassword = "a34y5h3ayh";

let env = process.env.NODE_ENV;

if (!env || (env!==undefined && env!==null && (env !== "development" && env!=="production"))) {
  throw `Environment not properly defined. Environment defined as ${env}`;
} else if (env === "development"){
} else if (env === "production"){
}

let config = {
    googleMaps: "AIzaSyCD7UYNAb9Moxb6eCfMJ7-YL0EhpXuayrw",
    cookieSecret: "myCookieSecretYall",
    curEnv: env,
    stripe: { 
        secret: "sk_test_51HxjZGBpF4mOeOLxBlsdwAvuC6okGaTmZ3wjwsm4j5NnTgzpREMyrFHwEHC7VXg3bfUj3gOXtwtdKtfPCofywwTX00K0Czzzbd"
    },
    aes:{
        secret: "myAesSecret",
        pass: "1234"
    },
    user:{
        secret: "superSecret"
    },
    dbCreds:{
        mongoUser: mongoUser,
        mongoPassword: mongoPassword,
        connection: "mongodb://localhost:27017/plannerApp"
    }
};

export {config};